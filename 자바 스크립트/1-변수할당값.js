// Number (숫자)
var Score = 90;

//String (문자열)
var text = '텍스트';

//Boolean (참 혹은 거짓)
var val = true;

//null (빈 값)
var empty = null;

//undefined (지정되지 않음)
var birth = undefined;

console.log(text);
